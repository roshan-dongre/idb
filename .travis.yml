sudo: false

language: python

python:
   - "3.6"

install:
    - pip install --upgrade pip
    - pip install coverage
    - pip install -r requirements.txt

before_script:
    - uname -a
    - printenv | sort

script:
    - date
    - sh run_tests.sh
